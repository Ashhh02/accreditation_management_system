# Core application dependencies
# Pinned to the exact versions verified in the working tree.
Django==6.0.8
whitenoise==6.12.0
python-dotenv==1.2.2
gunicorn==26.0.0
redis==8.1.0
daphne==4.2.3
channels==4.3.2
channels-redis==4.3.0
django-cleanup==9.0.0

# Production add-ons (uncomment for your deployment):
#   Redis-backed sessions/cache: django-redis
#   PostgreSQL backend:          psycopg[binary]